{
  "name": "data-sample",
  "version": "0.1.0",
  "description": "A sample react project showng how to grab data from server/json and filter it.",
  "private": true,
  "dependencies": {
    "react": "^16.0.0",
    "react-dom": "^16.0.0",
    "react-scripts": "1.0.14"
  },
  "devDependencies": {
    "json-server": "^0.12.0"
  },
  "scripts": {
    "start": "react-scripts start && concurrently \"npm run rest\" ",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "rest": "json-server --watch --port 3010 customerData.json"
  },
  "keywords": [
    "redux",
    "react"
  ],
  "author": "Pankaj Singh",
  "license": "MIT"
}